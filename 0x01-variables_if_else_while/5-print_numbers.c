#include <stdio.h>
/**
 * main - contain the programm
 *
 * Return: 0 in the main
 */
int main(void)
{
	int number = 0;

	while (number < 10)
	{
		printf("%d", number);
		number++;
	}
	printf("\n");
	return (0);
}
