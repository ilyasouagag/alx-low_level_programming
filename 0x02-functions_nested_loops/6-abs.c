#include "main.h"
/**
 * _abs - checks for lowercase character
 *
 * @int: parameter of function
 *
 * Return: always 0 (Success)
 */
int _abs(int)
{
	int a;

	if (a < 0)
		return (-a);
	return (a);
}
